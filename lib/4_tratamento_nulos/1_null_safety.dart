// ignore_for_file: file_names
String? nomeSuperior;
void main() {
  String nome = '';

  String? nomeNulo;

  if (nomeNulo != null) {
    nomeNulo.isEmpty;
  }

  nome.isEmpty;

  var nomeLocal = nomeSuperior;
  if (nomeLocal != null) {
    nomeLocal.isEmpty;
  }
}
